Пока что, мне так и не удалось получить какие-то результаты по проверке, ни во вкладке Findings на AppSec, ни, соответственно, в IDE (хотя бы не бесконечная загрузка, уже успех 🫠)

Я столкнулась со следующими проблемами и вот, как я пыталась их решить:

## Не найдены адрес отправления и токен этого запроса на отправление:

The variables required to send the report to the portal: 
(PORTAL_IMPORT_URL, PORTAL_TOKEN) were not provided.

Я сделала следующее, но для проверки с помощью инструмента Checkov, это не помогло: 	проверить и сохранить настройки	( Auditor URL = http://host.docker.internal:8080/), проверить совпадение Access Token с  токеном из файла auditor/.env,  проверить SSH-ключ (начинается с -----BEGIN OPENSSH PRIVATE KEY-----), проверить Workflow Settings (Internal Portal URL = http://host.docker.internal/), перезапустить сервисы (в папке auditor: docker compose down && docker compose up -d,  папке appsec-portal: sh stop.sh && sh run.sh)

## Проблемы, связанные с SSH-ключом 

В логах были следующие ошибки:
1. Error loading key "(stdin)": error in libcrypto — контейнер не смог распарсить ваш приватный ключ.
2. git@github.com: Permission denied (publickey). — доступ по SSH запрещён.

Попробую сгенерировать его заново: ssh-keygen -t ed25519 -f ~/.ssh/whitespots_key -N
Снова добавить новый ключ в настройки GitHub-аккаунта и Auditor/Config.
И перезапустить контейнеры в Auditor’е: docker compose down && docker compose up -d.

## Проблемы с анализом используемых в приложении библиотек (Trivy code deppendences)

Возможные проблемы и их решение: 
Проблема с источником БД: Увеличить таймаут-добавить переменную окружения TRIVY_TIMEOUT=10m для сканирования. 
Недостаточно места на диске для кэша: Очистить кэш в контейнере или на хосте в папке ~/.cache/trivy/.
Сетевые ограничения: Проверить настройки сети Docker.

## Сетевая проблема при использовании сканера Semgrep
(Semgrep Обнаруживает распространенные уязвимости (SQL-инъекции, XSS), небезопасные вызовы функций, устаревшие алгоритмы шифрования, а также ошибки логики, служит для статистического анализа - SAST)

Semgrep завис при попытке загрузить правила проверки (p/default) с официального сайта semgrep.dev. Из-за этого сканирование фактически не выполнилось, и итоговый отчёт оказался пустым (Report file has a size of: 0). Тем не менее, этот пустой отчёт был отправлен в портал (отсюда код 202).

Возможные проблемы:
Сеть контейнера не имеет доступа в интернет: нужно проверить сетевые настройки Docker’а - docker network inspect, пропинговать - docker compose exec <имя_контейнера_аудитора> ping -c 2 8.8.8.8. Увеличить таймаут для сканера. В Auditor Config добавить переменную в настройки сканера - SEMGREP_TIMEOUT.
Настроить Semgrep на использование локального файла правил вместо p/default. 

## Анализ корневой причины отсутствия результатов.

Несмотря на частичное решение проблем с передачей отчётов (код 202), ключевой этап — непосредственный статический анализ кода (SAST) и зависимостей (SCA) — не был выполнен ни одним из сканеров:
Checkov не смог получить метаданные из-за сетевого таймаута.
Semgrep не загрузил правила сканирования.
Trivy не скачал базу уязвимостей.
Code Downloader  не смог клонировать репозиторий из-за проблем с SSH-ключом.

Основная проблема — не создается первичный результат сканирования (артефакт с уязвимостями). Без этого артефакта последующие этапы пайплайна DevSecOps (агрегация результатов в портале, отображение в IDE) не имеют данных для работы. 

